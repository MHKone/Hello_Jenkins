pipeline {
    agent any
    stages {  // Move this block one level to the right
        stage('Clone'){
            steps {
                git 'https://github.com/MHKone/Hello_Jenkins.git'
            }
        }
    }
}
